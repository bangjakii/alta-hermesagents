[project]
name = "alta-hermes"
version = "0.1.0"
description = "Runtime Hermes ALTA: boot script desired-state (alta-database) -> profile Hermes, plus guardrail yang sengaja tinggal di kode."
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
    "psycopg[binary]>=3.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[project.scripts]
alta-hermes = "alta_hermes.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/alta_hermes"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
